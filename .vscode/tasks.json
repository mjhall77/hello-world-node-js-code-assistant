{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Bootstrap Workspace",
      "type": "shell",
      "command": "code --install-extension Continue.continue --force && npm start",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "runOptions": {
        "runOn": "folderOpen"
      },
      "presentation": {
        "reveal": "always",
        "panel": "dedicated",
        "clear": true
      }
    }
  ]
}
